import { Box, Typography } from '@mui/material';

export const GenericDeviceView = () => (
    <Box p={4} textAlign="center">
        <Typography variant="h5">Unknown Device Type</Typography>
        <Typography>Here be dragons.</Typography>
    </Box>
);
